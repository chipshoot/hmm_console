# Analyze Flutter Code

Run static analysis on the codebase:

```bash
flutter analyze
```

If there are issues, categorize them (errors vs warnings vs info) and fix errors first. For warnings, suggest fixes grouped by type.
